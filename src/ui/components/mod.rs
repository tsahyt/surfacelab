pub mod node_editor;
/// Components are "large widgets", that combine smaller widgets into a logical
/// component of the SurfaceLab user interface. The distinction between
/// components and widgets is somewhat arbitrary, since they both use the same
/// APIs.
pub mod top_bar;
